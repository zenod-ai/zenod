# EPIC: Mechanical Capture — Phylax Binding Table To Zenod

Status: SHIP-A passed uninterrupted; normal WhatsApp voice capture and grounded conversation live-proven; G1 awaiting Jordi
Created: 2026-07-29
Updated: 2026-07-30 (v2.2 frozen; SHIP-A passed; normal WhatsApp voice capture and grounded conversation live-proven; see Handoff Journal)
Repository: `zenod-ai/zenod`
Primary document: `docs/EPIC-MECHANICAL-CAPTURE.md`
GitHub issues: mint one issue per MC ticket at dispatch; link back into the Issue Ledger
Integration branch: `main`
Pinned base commit: `0fac451` (origin/main at authoring; RIV-1 TurnPlan compiler is merged and inert for this spine)
Last reconciled commit: `59732ddae8954b8c39678f35980d590bbca3687a`
Active spine steward: Codex delivery manager (`/root`, assigned 2026-07-29)
Planner: Claude (spine manager session with Jordi, 2026-07-29)
Worker: delivery manager with issue-bound parallel worktree workers
Tester: delivery manager walks first; Jordi walks the identical journey, including his two real pending voice notes

Supersession: this spine supersedes `docs/EPIC-RING-INTENT-AND-VOICE-MEMORY.md` for execution (its typed-intent design is preserved as the blueprint for a future Ring era — see D12) and takes over the open journey of `docs/EPIC-WHATSAPP-ZENOD-STABILIZATION.md` (local, unlanded, now ON HOLD).

## Role Bindings

| Identity | Assignment | Scope | Authority | Handoff |
|---|---|---|---|---|
| Planner | Claude spine-manager session + Jordi | This spine | Preserve intent, decisions, waves, gates. Resolve only scope/acceptance divergence raised by the delivery manager. | Spine edits, journaled. |
| Delivery manager (epic worker) | Codex `/root` | This spine, all MC tickets | MANAGER MANDATE: mint issues, dispatch parallel worktree workers, review and integrate PRs, deploy via existing CI, walk the SHIP journey personally, iterate to one clean pass. Stops only at SHIP, a named Human Gate, or budget expiry. Asking anything answerable from this spine is a defect. | Heartbeats, journal entries, exact candidate SHA, test package. |
| Ticket worker | One worker per MC ticket | Exactly one MC ticket | FIRST ACTION: `git worktree add ../wt-<ticket> -b codex/mc-<n>-<slug> 0fac451` (wave 2 re-pins per Branch And Integration). Touch only that ticket's file surface. Scratch-writing anything marked PORT/DUPLICATE fails review. | PR, base/latest SHA, focused tests, risks, next action in the bound issue. |
| Reviewer | Separate from each ticket worker | One PR at a time | Scope, multitenant cleanliness (D7), safety, tests, regression risk. Reject any PR that adds interpretation to the capture path (D1) or peer clients to Zenod (D2). | Approve/block in the PR. |

## Write Scope

- The active steward reconciles and commits this spine.
- Ticket workers write detailed state to their bound GitHub issue only.
- Each ticket worker changes only the files listed in its Issue Ledger row, on its own branch/worktree. `git checkout`/`git switch` in a shared clone is a defect (branch ransom).

Read-only linked spines: `docs/EPIC-P-PHYLAX-SPRINT.md` (channel ownership, custody, typed downstream handoff), `docs/EPIC-Z-NIGHT-SPRINT.md` (memory ownership, receipt semantics), `docs/EPIC-RING-INTENT-AND-VOICE-MEMORY.md` (SUPERSEDED — context only, do not execute), `docs/EPIC-WHATSAPP-ZENOD-STABILIZATION.md` (ON HOLD parent — incident context), `docs/evidence/whatsapp-zenod-stabilization-2026-07-29/` (root-cause, counter-proposal, and sink-design HTML docs; the sink-design doc is the CANONICAL picture for this spine, steward: planner).

## Authority By Artifact

- This spine is authoritative for mission, scope, D1–D19, waves, budgets, gates, epic acceptance, dependencies, and rollup state.
- Each linked GitHub issue is authoritative for detailed execution state of its MC ticket.
- Branches, PRs, and code are authoritative for the implementation that exists.
- Validation evidence is authoritative only for the named environment and exact commit recorded with it.
- The active spine steward reconciles drift without overwriting observable implementation or test evidence.

## Bootstrap Map

- Goal and desired outcome: [Mission](#mission).
- Epic acceptance and live journey: [Definition Of Done](#definition-of-done).
- Exclusions and frozen surfaces: [Non-Goals](#non-goals).
- Execution snapshot: [Current State](#current-state).
- Architecture and required context: [Architecture And Context](#architecture-and-context) plus the canonical sink-design HTML named above.
- Binding decisions and traps: [Decisions](#decisions).
- Executable tickets, owners, branches, bases, budgets, and next actions: [Issue Ledger](#issue-ledger).
- Integration sequencing: [Branch And Integration](#branch-and-integration).
- Required human input: [Human Gates](#human-gates).
- Durable reconciliation history: [Handoff Journal](#handoff-journal).
- Unresolved scope questions: [Open Questions](#open-questions).

## Mission

Build the assistant, with capture guaranteed underneath it (design accepted by Jordi 2026-07-29 evening — see D10/D17/D18; the earlier era framing is retired). Two structural lanes in one WhatsApp thread:

CAPTURE (mechanical, guaranteed): a standalone voice note, image, or document becomes exactly one committed Zenod memory with one host-templated receipt — per-tenant binding table, no model call, no regex guard, no Ring on this path. The receipt invites: "reply to this message to discuss or act on it."

ASSISTANT (Ring, conversational): all ordinary text goes to Ring — the delegator. Any text OR VOICE sent as a WhatsApp reply to a capture receipt also goes to Ring, carrying that capture's evidenceRef (a structural fact, not interpretation). Ring reads memory via Zenod, asks clarifying questions when unsure, and fulfils nothing itself: it delegates to final agents (Zenod stores/answers, Callisthenes drafts-then-sends). External actions exist only as held drafts released by explicit approval. Ring also receives each capture's ticket (summary + evidenceRef, never the full transcript) so the conversation knows what just landed — the user experiences one assistant.

The topology stays coherent: doors route by structure, memory serves and never initiates, Ring delegates and never executes world effects, actuators stay held-draft guarded.

## Definition Of Done

### SHIP-A — capture (walked by the delivery manager on the LIVE deployment, loop: walk → first breakage → fix exactly that → deploy → walk again FROM STEP 1, until one uninterrupted clean pass, screenshot per step; then Jordi walks the identical journey with his two real pending voice notes)

1. Send a fresh long (15–25 min) voice note to the tenant-zero WhatsApp number.
2. An immediate progress message arrives within seconds ("got it — transcribing").
3. STT succeeds, or fails loudly within its deadline with an honest reply — never a silent fallback.
4. Phylax invokes `store_memory` carrying the durable idempotency key (tenant + provider message ID, D14/MC-10); resending the same message creates no second memory.
5. Phylax persists the returned job ID BEFORE awaiting it, and polling survives a Phylax restart mid-job (restart Phylax during filing on at least one lap; the receipt still arrives).
6. Only a terminal Zenod receipt renders "Saved": recap, filed page(s), clickable commit link, and the librarian's question if filing was unclear. Open the commit link: the verbatim transcript is in the vault Log with evidence anchors.
7. Reply by text: "what were the open questions in that note?" — Phylax passes the last capture's `evidenceRef` into read-only `ask_brain` (MC-11); the answer cites that note.
8. Verify the channel token structurally: `tools/list` on the channel binding's token exposes memory tools only — no action tools exist to misuse (D3/D15). No linguistic refusal is claimed or tested.
9. Kill the WhatsApp socket / stall the connection phase on the VPS. The lifecycle watchdog (D6) detects and restarts within its deadline; a fresh short voice note lands with a receipt.
10. Leave the tenant idle for a natural quiet window during the pass (or simulate one): NO restart occurs from ordinary human silence — inactivity is healthy (D6).

Terminal artifact: "I manually walked steps 1–10 on <deployed SHA>. URL + screenshots. Now you test." Jordi's hand test (including his two real voice notes landing in memory) closes SHIP-A (gate G1). SHIP-A does NOT wait for SHIP-B: the two real voice notes land as soon as SHIP-A passes.

### SHIP-B — the assistant journey (walked the same way, on the same deployment, after SHIP-A is approved; separate gate G1b)

1. Reply BY VOICE to a capture receipt: "extract the proposed actions from this note and draft the tweet." Phylax transcribes and routes to Ring with that capture's evidenceRef attached (structural reply-context, D17 — no language interpretation in Phylax).
2. Ring pulls the referenced content from Zenod, and produces a held tweet draft via Callisthenes. Nothing publishes. The reply shows the exact draft and asks.
3. Reply "yes, publish that." Ring resolves the exact held draft (existing standing-approval contract); Callisthenes publishes; a verified permalink receipt arrives. No other phrasing of success exists (D15 truth boundary).
4. Reply to the receipt with an ambiguous instruction ("do the thing with the second one"). Ring asks ONE clarifying question instead of guessing (D18).
5. Send a text action request with no reply context: "post a tweet saying hi." Ring routes it the same way: held draft → approval → receipt. No draft is ever auto-sent.
6. Send another STANDALONE voice note mid-conversation: it stores mechanically as a new memory (tenant `voiceDefault=capture`) — capture never gets smarter, conversation never gets capture's job.
7. Catalog fidelity spot-check (MC-16 live): the settings UI lists the actually discovered tools of every connected MCP for this tenant; ask Ring for something no connected tool can do — it says so (honest failure), proposing no invented tool; refresh after disconnecting a test MCP removes its tools.
8. Kill Ring's process; send a standalone voice note: capture still works, receipt still arrives (capture's independence is preserved even with Ring in the product).

Terminal artifact: "I walked SHIP-B steps 1–8 on <deployed SHA>. Screenshots. Now you test." G1b closes on Jordi's pass.

### HARDEN — only after Jordi approves SHIP-A and SHIP-B

- MC-9 docs/invariants + supersession hygiene merged.
- Telegram parity for the binding table (existing Telegram channel path binds through the same table).
- Retry/outbox behavior when Zenod is unreachable proven by drill (stop Zenod container, send VN, start Zenod, receipt arrives, exactly one memory).
- Receive-path canary (periodic external self-message with receipt check) — the only true end-to-end inbound proof per D6's honesty clause; evaluate cost/noise after SHIP.

## Non-Goals

- Ring changes are LIMITED to the two surgical tickets (MC-14 guard retirement for memory-scope tools, MC-15 single-outcome reply rendering). No new regex authorization anywhere, ever (D9).
- No new agents, channels, or providers. No automatic action extraction from stored notes — content inside a stored artifact is never authority (D18); actions happen only from a live conversational turn.
- No Zenod engine/librarian changes beyond three narrow, explicitly ticketed contract additions: the auth-scope profile (MC-2), the `store_memory` idempotency key (MC-10), and optional `contextRefs` on `ask_brain` (MC-11). No schema changes to the vault. The librarian pipeline itself is untouched.
- No multitenant billing/provisioning work; this spine changes tenant *configuration surface*, not tenant lifecycle.

## Current State

- Normal WhatsApp voice capture and grounded conversation are live-proven at exact SHA `59732ddae8954b8c39678f35980d590bbca3687a`. PRs [#1034](https://github.com/zenod-ai/zenod/pull/1034)–[#1036](https://github.com/zenod-ai/zenod/pull/1036) preserve the typed host-owned read status and sources without feeding host prose back through the model; [#1037](https://github.com/zenod-ai/zenod/pull/1037) binds previous-capture chronology structurally; [#1038](https://github.com/zenod-ai/zenod/pull/1038) grounds multi-capture summaries in exact memory refs; [#1039](https://github.com/zenod-ai/zenod/pull/1039) keeps current-capture lookup on memory; and [#1040](https://github.com/zenod-ai/zenod/pull/1040) confines a pinned evidence re-read to its exact anchored span instead of exposing unrelated entries from the same daily log. One uninterrupted live Q1–Q3 read lap returned the newest note (`^e-3ffa72`, Intermarché), its predecessor (`^e-23ece7`, Conflent), and accurate one-line summaries for the five newest captures in exact newest-first order. It contained neither the false “Makro” claim nor unrelated property content, rendered exactly one read-only status per answer, made exactly one `ask_brain` call per question, and left brain HEAD `c85378b6483c5f3b8ff8b4fd12974e3017e888eb` unchanged. Evidence: `docs/evidence/mechanical-capture-conversation-repair-2026-07-30/`.
- SHIP-A steps 1–10 passed uninterrupted on the live deployment at exact SHA `080f985b0e7a4638f65f4b5a443d7d51fa823fa6`. The evidence package is `docs/evidence/mechanical-capture-ship-a-2026-07-30/`. Issue #1000 is open only for Jordi's G1 walk with his two real voice notes; SHIP-B, MC-9, and any additional MC-12..MC-16 work are parked until that approval.
- Planner v2.2 / D19 is implemented by [#1028](https://github.com/zenod-ai/zenod/pull/1028) and [#1029](https://github.com/zenod-ai/zenod/pull/1029). The lexical answer sanitizer is gone; `ask_brain` uses typed `answer_content`, and the host renders the typed read-only outcome. The exact `^e-a7f53e` live regression passed with “show the saved pages” intact. Issue #1004 is closed.
- Wave 1 base: `0fac451` — all four tickets integrated without rebasing.
- Wave 2 base: `2ace3bd99d3e8810cce444669cfa3ca2df414bee` — integrated Wave 1, pinned. No Wave-2 branch may rebase; Wave 3 re-pins only after Wave 2 integration.
- Wave 3 is code-complete. MC-12 through MC-16 are independently reviewed, CI-green, and integrated on `main`; final SHIP-A candidate `080f985` passed the runtime-image boot smoke, published to GHCR, and is live on Zenod, Ring, and Phylax.
- Live SHIP preflight is green for tenant `github-63050995`: Zenod's replaceable `memory-channel` credential exposes exactly the six D3 memory tools; Ring's replaceable `capture-ticket` credential exposes exactly `record_capture_ticket`; Phylax targets canonical bearer endpoints at Zenod and Ring with `voiceDefault=capture`; and Ring's Zenod, Callisthenes, and Phylax wallet entries are authenticated, catalog-ready, and bound to explicit tenant-scoped trusted profiles. All legacy credentials exposed during private diagnostics were rotated before the walk.
- MC-8 deployment preflight found the first breakage before a live lap: the GHCR publish smoke crashed because the runtime image omitted `packages/core/node_modules`. The one-line packaging correction passed independent review and CI in PR #1023, integrated at `4c1ca5e`, and exact main publish run [30484461054](https://github.com/zenod-ai/zenod/actions/runs/30484461054) passed runtime boot smoke and pushed the image.
- Wave 1 dispatch began 2026-07-29 17:23 CEST. Development was halted on Jordi's instruction, then resumed from preserved worktrees at 18:20 CEST after Planner v2.1 was read and issue contracts were reconciled. MC-1 merged first at `3423c1d`; MC-3 merged next at `b911bf8`; MC-4 merged at `5c52c1b` after a restart-loop P1 was fixed and independently re-reviewed; MC-2 merged last at `2ace3bd` after its hosted-route and non-MCP-auth escape P1s were fixed and independently re-reviewed.
- All sixteen MC issues are minted and linked in the Issue Ledger. The GitHub app lacked issue-write permission, so the steward used the authenticated `gh` fallback permitted by the GitHub operating skill.
- `main` contains the merged RIV-1 typed TurnPlan compiler; it is not on the capture path and MUST NOT be extended under this spine.
- Phylax downstream is already a generic MCP client: `callPeerTool(url, token, tool, args)` with per-tenant encrypted `downstreamUrl`/`downstreamToken` (`phylaxTenantSettings.ts`). The single Ring-ism is the hardcoded literal `tool: "chat_with_ring"` (`phylaxChannels.ts` ~lines 90 and 492) and the `callDownstream` injection point (~line 525).
- Zenod MCP already exposes `store_memory` (content/hints/verbatim → jobId), `get_task_result` (terminal receipt: evidence ref, pages, commit SHA, question), `ingest_memory`, `search_memory`, `ask_brain` (`packages/server/src/mcp.ts`).
- Existing branches to harvest (PORT candidates, verify before use): `fix/whatsapp-fail-loud`, `fix/drop-whisper-compile`, `codex/phylax-reconnect-open-watchdog` (review-blocked: evidence, not approved code), merged `W-P4` media coalescing (dedupe pattern).
- Incident context: 2026-07-29 root-cause doc in `docs/evidence/whatsapp-zenod-stabilization-2026-07-29/`.

## Architecture And Context

The accepted picture (canonical: `docs/evidence/whatsapp-zenod-stabilization-2026-07-29/phylax-mcp-sink-and-experience-design.html`):

```
WhatsApp/Telegram → Phylax (custody, idempotency, watchdog)
  → STT (fail loud)
  → BINDING TABLE (per-tenant config: turn type → MCP tool + arg template)   ← a lookup, not a decision
      voice_note → zenod.store_memory { content: transcript, verbatim: true, hints }
      text       → zenod chat (memory-scoped token)         ← era-1 default
      image/doc  → zenod.ingest_memory
  → poll get_task_result to terminal (deadline; queued ≠ saved)
  → receipt template reply (recap · pages · commit link · librarian question)
```

Two lanes, one thread, one assistant: capture is guaranteed and mechanical; conversation is Ring, which reads what capture committed and receives each capture's ticket (summary + evidenceRef). The structural routing law (D17): standalone media → capture; ordinary text → Ring; text or voice REPLYING to a capture receipt → Ring + that evidenceRef. Phylax never interprets language — it reads message type and WhatsApp reply-context, both structural facts. Zenod is a leaf — it never calls peers (D2). Callisthenes is Ring's actuator, never user-facing.

**Wave 1 (parallel, disjoint file surfaces):** MC-1 ∥ MC-2 ∥ MC-3 ∥ MC-4.
**Wave 2 (parallel after wave 1 integrates; re-pin):** MC-5 ∥ MC-6 ∥ MC-7 ∥ MC-10→MC-11 (one worker, sequential — both touch `mcp.ts`).
**Wave 3 (assistant, after wave 2 integrates; re-pin):** MC-12 ∥ MC-13 ∥ MC-14 ∥ MC-15 ∥ MC-16 (MC-12 follows MC-5 on `phylaxChannels.ts`, so it starts once MC-5 merges).
**Journey loops:** MC-8 — SHIP-A after wave 2, SHIP-B after wave 3 — owned by the delivery manager personally.
**HARDEN:** MC-9 + drills, after the SHIP gate.

## Decisions

| ID | Decision | Absence rule / trap note |
|---|---|---|
| D1 | Capture is mechanical: no routing or intent model call IN PHYLAX, and the capture path MUST NOT import `taskingPolicy.ts` or any interpreter. Zenod's librarian classifier/composer model calls after `store_memory` are legitimate peer-owned filing, not a violation. A PR adding interpretation to the Phylax capture path is rejected on sight. | — |
| D2 | Zenod never calls peers and acquires no peer tokens, ever. Memory is a leaf. | If a ticket seems to need Zenod calling out, the design is wrong — journal and raise to planner. |
| D3 | Channel bindings authenticate with a memory-scoped tool profile: `store_memory`, `search_memory`, `ask_brain`, chat, `get_task_result`, `ingest_memory` only. A full-surface token on a channel binding is a defect. | If no scoping mechanism exists, MC-2 builds the smallest one (per-token tool allowlist); do not defer. |
| D4 | Voice notes store by default, `verbatim: true`. Git history is the undo. When in doubt, store. | — |
| D5 | STT failure replies to the user within seconds. The local Whisper fallback is removed from the channel path. Any fallback that changes latency by an order of magnitude must fail loudly instead of degrading. | — |
| D6 | The watchdog observes TRANSPORT LIFECYCLE, never user traffic — and does NOT duplicate keepalive logic: Baileys already implements its own protocol ping/pong and closes the connection when responses stop; MC-4 CONFIGURES and TESTS that native keepalive rather than rebuilding it. MC-4 adds only: bounded startup/handshake phases (deadline → restart), a native connection-loss/terminal event → bounded restart, and a Phylax worker/event-loop heartbeat. No user messages while connected = HEALTHY — ordinary human silence must never trigger a restart. HONESTY CLAUSE: keepalive proves the transport, not the complete inbound-message path — a stuck application handler can coexist with healthy pings. Only a real message receipt (SHIP laps) or an external canary proves end-to-end receiving; the canary is HARDEN scope, and this spine does not claim ping/pong alone catches every zombie. | Traps: "time since last receive event" restart-loops during quiet hours — rejected. Re-implementing Baileys' ping loop in Phylax — rejected (fragile duplicate lifecycle code). |
| D7 | **Multitenant law (Jordi, 2026-07-29): every change ships as per-tenant configuration with sane defaults. No tenant-zero special cases in code, no hacks to get one user working. Tenant zero is only the first tenant to flip a config flag.** | Reviewer checks this on every PR. |
| D8 | MC-1 ships as a no-op: the default binding reproduces today's behavior (`chat_with_ring`). Behavior changes only when a tenant's binding is flipped (MC-7 default rows + tenant-zero flip during MC-8). | — |
| D9 | Guard law — THE MODEL PROPOSES, THE HOST AUTHORIZES. Ring's model proposes exactly one discovered tool + arguments; a deterministic host policy then authorizes by RISK, derived from MCP annotations + the connection's trusted profile — never from tool names, peer special cases, mutation phrases, or a second language classifier. Ladder: private + tenant-scoped + non-destructive + closed-world → may execute; public/external/destructive/financial → exact held-action approval; missing or unknown risk metadata → hold or clarify (fails closed). Annotations are hints from the server; the per-connection TRUSTED PROFILE (host-side config) decides how far they are believed. No positive-intent regex authorization is ever added. | Traps: the incident — a regex vetoing the model's correct store call; AND the inverse — auto-executing any newly connected mutation whose description persuades the model. Both die here. |
| D10 | Era framing RETIRED (Jordi, 2026-07-29 evening): this spine builds the assistant, not a capture-only stopgap. Capture stays mechanical underneath it; the assistant lane is Ring per D17/D18. Embedded actions in stored notes remain content (see D18). | — |
| D11 | Credentials and endpoints are READ from the live tenant settings surface/DB. Never re-created, never requested from Jordi. | — |
| D12 | The RIV typed-intent design is preserved as the blueprint for a future Ring era. MC-9 rewrites `docs/EPIC-RING-INTENT-AND-VOICE-MEMORY.md` Status to `SUPERSEDED by docs/EPIC-MECHANICAL-CAPTURE.md — do not execute from this document`. | — |
| D13 | Async receipts: `queued` is never rendered as saved. Poll deadline 4 minutes for long notes; on deadline, reply honestly ("still filing — I'll confirm") and keep polling in background; the eventual terminal state is delivered proactively. | Trap from incident: placeholder receipts. |
| D14 | Exactly-once is Zenod-owned, not Phylax-owned: `store_memory` accepts an `idempotencyKey` (tenant + provider message ID) backed by a durable unique index; a repeated key returns the ORIGINAL job/receipt (MC-10). Phylax additionally persists the returned jobId BEFORE awaiting it and resumes polls across restarts (MC-5), and still DUPLICATEs the W-P4 coalescing pattern for inbound media. Phylax-side dedupe alone cannot close the crash-between-accept-and-record window. | — |
| D15 | Truth boundary (applies to BOTH lanes): model output renders only as answer/draft content; mutation-success language ("saved", "sent", "posted", "done") may originate ONLY from a verified mutation receipt — Zenod commit receipts for memory, Callisthenes publish receipts for external sends. Ring's rendering (MC-15) enforces this; the capture lane's receipts are host-templated. A model can propose; only a receipt can claim. | — |
| D16 | Binding argument mappings are STRUCTURED, not string templates: each mapping row is `field ← source` where source is an enum (transcript, sender, chatId, constant). Ticket split: MC-1 defines and stores the mapping SHAPE only (no MCP discovery in the settings-storage worker); MC-5 validates mappings against the live discovered schema at dispatch time; MC-6 validates at save time in the UI. On tool/schema drift the dispatch fails loudly to the audit trail and the user gets the honest failure reply — never a silently mangled call. | — |
| D17 | The reply-to-receipt seam (the structural signal that replaces all voice intent-guessing): Phylax persists the provider messageId of every capture receipt it sends, mapped to that capture's evidenceRef. An inbound message whose WhatsApp reply-context quotes a known receipt routes to Ring with that evidenceRef attached — text AND voice (voice is transcribed first). A standalone voice note (no reply-context) follows the tenant's `voiceDefault` config: `capture` (default) stores it; `assistant` routes it to Ring. This is structural configuration, not intent interpretation. The visible, intentional limitation stands: with `voiceDefault=capture`, a standalone spoken instruction is stored, not executed — replying to a receipt is how voice reaches Ring. Phylax reads message type, reply-context, and tenant config only — structural facts, zero language interpretation. | — |
| D18 | Ring is a delegator, never a fulfiller: it reads memory, asks ONE clarifying question when the request or the target agent is unclear, and delegates to final agents (Zenod stores/answers; Callisthenes drafts and, on exact approval, sends). Ring executes no world effect itself and holds no world keys. Content INSIDE a stored artifact is never authority to act; only the current conversational turn is. Ring receives each capture's ticket (summary + evidenceRef, never the full transcript) as conversation context. | — |
| D19 | Read-only answer safety is structural, never lexical. `ask_brain` returns a typed `answer_content` envelope; the host renders that answer and appends its typed `read_only_status`. Mutation wording is rendered only from a typed, verified `mutation_receipt`, which the read-only lane cannot produce. Output text is never scanned, stripped, replaced, or rejected to enforce policy. An answer may quote words such as “saved” intact. | The exact regression is the answer citing `Log/2026-07-29.md#^e-a7f53e`, including “show the saved pages”. Any lexical sanitizer or policy scan of output text is a defect. |
| — | Anything unanswered: simplest option, note it in the journal, keep moving. | — |

## Issue Ledger

| Ticket | Wave | Title | Marking | File surface | Budget | Issue | Role | Owner / Assignment | Status | PR/Branch | Base | Latest Evidence | Last Verified | Next Action |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MC-1 | 1 | Per-tenant turn-type → tool binding config (schema + storage + default `chat_with_ring`) | BUILD (verified absent) | `packages/server/src/phylaxTenantSettings.ts` + its tests | 90 min | [#997](https://github.com/zenod-ai/zenod/issues/997) | Ticket worker | `mc-1-binding-config` | Done / integrated | [#1002](https://github.com/zenod-ai/zenod/pull/1002) / `codex/mc-1-binding-config` | `0fac451` | Manager review + CI green; squash merge `3423c1d` | 2026-07-29 18:27 CEST | Integrated; D16 mappings and D17 `voiceDefault` are Wave 2 base inputs. |
| MC-2 | 1 | Memory-scoped MCP tool profile for channel tokens (per-token tool allowlist enforced at the MCP endpoint) | BUILD; hosted-route trace found no usable scope mechanism, so built the smallest distinct token profile/alias per D3 | minimum chassis token-profile storage/auth-context/wiring + Zenod profile mapping/tests | 90 min | [#994](https://github.com/zenod-ai/zenod/issues/994) | Ticket worker | `mc-2-memory-token` | Done / integrated | [#1011](https://github.com/zenod-ai/zenod/pull/1011) / `codex/mc-2-memory-token` | `0fac451` | Exact-six hosted list/call; profiled credentials MCP-only; escape regression 401; 89+45 focused tests, typechecks, CI, independent review; squash merge `2ace3bd` | 2026-07-29 18:53 CEST | Integrated; D3 scoped-token mechanism is on `main`. |
| MC-3 | 1 | STT fail-loud: remove local Whisper fallback from the channel path; immediate honest failure reply | PORT from `fix/whatsapp-fail-loud` + `fix/drop-whisper-compile` (verify state first) | transcription path (`channelAudio.ts` + STT call sites) | 60 min | [#1001](https://github.com/zenod-ai/zenod/issues/1001) | Ticket worker | `mc-3-stt-fail-loud` | Done / integrated | [#1010](https://github.com/zenod-ai/zenod/pull/1010) / `codex/mc-3-stt-fail-loud` | `0fac451` | Manager re-ran 79 tests + typecheck; CI green; squash merge `b911bf8` | 2026-07-29 18:27 CEST | Integrated; fail-loud channel STT is on `main`. |
| MC-4 | 1 | Transport-lifecycle watchdog per D6 (v1.2): configure + test Baileys' NATIVE keepalive (do not re-implement); bounded startup/handshake deadlines; native connection-loss/terminal event → bounded restart; Phylax worker/event-loop heartbeat; compose healthcheck/restart. User-message recency is NOT a health signal; no keepalive duplication. | PORT evidence from `codex/phylax-reconnect-open-watchdog` (harvest the liveness signal, discard the choreography) + Baileys socket source as reference | phylax runtime health surface + `docker-compose.phylax.yml` | 90 min | [#995](https://github.com/zenod-ai/zenod/issues/995) | Ticket worker | `mc-4-receive-watchdog` | Done / integrated | [#1012](https://github.com/zenod-ai/zenod/pull/1012) / `codex/mc-4-receive-watchdog` | `0fac451` | P1 transient/terminal restart-loop fixed; independent re-review 41/41; full 820 tests + CI green; squash merge `5c52c1b` | 2026-07-29 18:45 CEST | Integrated; D6 watchdog is on `main`. |
| MC-5 | 2 | Dispatch honors the binding table with STRUCTURED validated arg mappings (D16); sends `idempotencyKey` (D14); persists jobId BEFORE awaiting; async poll to terminal (D13) resumable across Phylax restarts; receipt template (recap · pages · commit · question relay); records last successful capture's `evidenceRef` per conversation for MC-11 handoff | BUILD + DUPLICATE W-P4 coalescing and the existing `get_task_result` poll convention | `packages/server/src/phylaxChannels.ts` | 120 min | [#996](https://github.com/zenod-ai/zenod/issues/996) | Ticket worker | `mc-5-binding-dispatch` | Done / integrated | [#1013](https://github.com/zenod-ai/zenod/pull/1013) / `codex/mc-5-binding-dispatch` | `2ace3bd99d3e8810cce444669cfa3ca2df414bee` | Independent review + CI green; squash merge `7c11460` | 2026-07-29 19:49 CEST | Structured production dispatch, durable terminal polling, exact-once handoff, receipt/evidence seam, and D13 deadline custody integrated. |
| MC-6 | 2 | Settings UI: discover (`tools/list`) and bind — dropdowns populated by discovery, never typed by hand | BUILD | UI + test; steward-authorized minimum `phylaxUnit.ts` discovery proxy, server-only tenant credential accessor if required, focused server tests | 90 min | [#999](https://github.com/zenod-ai/zenod/issues/999) | Ticket worker | `mc-6-binding-settings-ui` | Done / integrated | [#1014](https://github.com/zenod-ai/zenod/pull/1014) / `codex/mc-6-binding-settings-ui` | `2ace3bd99d3e8810cce444669cfa3ca2df414bee` | Independent review + CI green; squash merge `c264733` | 2026-07-29 19:21 CEST | Secure tenant-side discovery, exact schema mappings, drift rejection, and save-time type/enum/const compatibility integrated. |
| MC-7 | 2 | Routing-law defaults per D17: standalone VN→`store_memory` verbatim, standalone media→`ingest_memory`, ordinary text→Ring binding, reply-to-receipt→Ring binding + evidenceRef (detection itself is MC-12); host-templated receipt copy includes the "reply to this message to discuss or act on it" line; per-tenant flip mechanism | BUILD | binding defaults + reply templates (surface established by MC-1/MC-5) | 60 min | [#993](https://github.com/zenod-ai/zenod/issues/993) | Ticket worker | `mc-7-routing-defaults` | Done / integrated | [#1015](https://github.com/zenod-ai/zenod/pull/1015) / `codex/mc-7-routing-defaults` | `2ace3bd99d3e8810cce444669cfa3ca2df414bee` | Independent review + CI green; squash merge `4e99faf` | 2026-07-29 19:22 CEST | G2-approved global defaults; structural media sources fixed before merge; production consumers remain MC-5 gate. |
| MC-8 | 3+ | Journey loops on the live deployment — SHIP-A after wave 2, SHIP-B after wave 3 — owned by the delivery manager personally | — | deploy + walk | until clean pass | [#1000](https://github.com/zenod-ai/zenod/issues/1000) | Epic worker / tester | Delivery manager `/root` | SHIP-A passed; normal WhatsApp voice capture and conversation live-proven / G1 awaiting Jordi | [#1023](https://github.com/zenod-ai/zenod/pull/1023), [#1028](https://github.com/zenod-ai/zenod/pull/1028)–[#1040](https://github.com/zenod-ai/zenod/pull/1040) merged; deployed `59732dd` | `59732ddae8954b8c39678f35980d590bbca3687a` | SHIP-A steps 1–10 passed uninterrupted on `080f985`; the final uninterrupted WhatsApp Q1–Q3 read lap passed on `59732dd` with exact latest/predecessor evidence (`^e-3ffa72`, `^e-23ece7`), accurate five-note summaries, no false Makro/property content, one status and one memory read per answer, and unchanged brain HEAD after reads. Evidence in `docs/evidence/mechanical-capture-ship-a-2026-07-30/` and `docs/evidence/mechanical-capture-conversation-repair-2026-07-30/`. | 2026-07-30 16:34 CEST | Jordi sends his two voice notes now and reports the exact interaction result; SHIP-B remains parked pending G1. |
| MC-9 | HARDEN | Invariants doc (D1/D2/D3/D7/D10 as standing law), RIV spine supersession edit on `main` (D12) | BUILD (docs-only, no ceremony) | `docs/` | 45 min | [#998](https://github.com/zenod-ai/zenod/issues/998) | Ticket worker | Unassigned until G1 | Blocked by G1 | Docs branch pending | Post-SHIP `main` pending | Issue minted | 2026-07-29 17:23 CEST | Start only after Jordi approves SHIP. |
| MC-10 | 2 | Zenod contract: `store_memory` accepts optional `idempotencyKey`; durable unique index on (tenant, key); repeated key returns the ORIGINAL job/receipt, never a second job (D14). Narrow: no librarian changes. | BUILD | `packages/server/src/mcp.ts`, the task-job store implementation (`taskJobStore`/ingest store), and shared MCP schema types | 90 min | [#1007](https://github.com/zenod-ai/zenod/issues/1007) | Ticket worker (shared with MC-11, sequential) | delivery manager `/root` as ticket worker | Done / integrated | [#1016](https://github.com/zenod-ai/zenod/pull/1016) / `codex/mc-10-store-idempotency` | `2ace3bd99d3e8810cce444669cfa3ca2df414bee` | Independent review + CI green; squash merge `4cf5d64` | 2026-07-29 19:27 CEST | Durable tenant-key replay, atomic leased claims, rolling-overlap recovery, stale-owner fencing, and safe shutdown integrated. |
| MC-11 | 2 | Zenod contract: `ask_brain` accepts optional `contextRefs` (evidence refs); answers ground on the referenced evidence first. Phylax passes the last capture's `evidenceRef` (from MC-5). Read-only; narrow. D19 correction: typed `answer_content`; no lexical output-policy filtering. | BUILD + v2.2 regression correction | `packages/server/src/mcp.ts`, shared `ask_brain` schema/types, answer grounding call site, typed host outcome boundary | 60 min + final regression correction | [#1004](https://github.com/zenod-ai/zenod/issues/1004) | Ticket worker (shared with MC-10, sequential) | delivery manager `/root` after MC-10 | Done / integrated / live-proven | [#1017](https://github.com/zenod-ai/zenod/pull/1017), [#1028](https://github.com/zenod-ai/zenod/pull/1028), [#1029](https://github.com/zenod-ai/zenod/pull/1029) | `4cf5d64b5f27c6e2c6902c7f64d3e4ada78d576f` | Typed envelope and typed host rendering merged at `080f985`; exact `Log/2026-07-29.md#^e-a7f53e` live answer retained “show the saved pages”; no new lexical-policy pattern in the fix diff | 2026-07-30 01:14 CEST | Closed. D19 live regression evidence is in the SHIP-A evidence package. |
| MC-12 | 3 | Reply-to-receipt seam (D17): persist capture-receipt provider messageIds → evidenceRef map; detect WhatsApp reply-context on inbound text AND voice; route to the Ring binding with evidenceRef attached; standalone media unaffected | BUILD | `packages/server/src/phylaxChannels.ts` + receipt store (after MC-5 merges — shared file) | 90 min | [#1005](https://github.com/zenod-ai/zenod/issues/1005) | Ticket worker | `/root/mc_1_binding_config` | Done / integrated | [#1020](https://github.com/zenod-ai/zenod/pull/1020) / `codex/mc-12-receipt-reply` | `7c11460d3c8be04c6a99c6fd2dd52caa7bded444` | Independent review + CI green; squash merge `c288bec` | 2026-07-29 21:10 CEST | Integrated; durable receipt-ID mapping, structural text/voice reply routing, and exact restart recovery are on `main`. |
| MC-13 | 3 | Capture ticket to Ring: after each terminal capture receipt, post the capture event (summary + evidenceRef, NEVER the full transcript) into the tenant's Ring conversation context (D18) | BUILD | Ring conversation-state surface (identify at ticket start; not `phylaxChannels.ts`) | 60 min | [#1003](https://github.com/zenod-ai/zenod/issues/1003) | Ticket worker | `/root/mc_3_stt_fail_loud` | Done / integrated | [#1022](https://github.com/zenod-ai/zenod/pull/1022) / `codex/mc-13-ring-capture-ticket` | `7c11460d3c8be04c6a99c6fd2dd52caa7bded444`; merged `4c1ca5e` without rebase | Exact `b62f21b`: independent review + fresh CI green; squash merge `1a9fd83` | 2026-07-29 21:50 CEST | Integrated; distinct scoped Ring route, exact memory-authority verification, durable identity/recovery, and real background terminal wake are on `main`. |
| MC-14 | 3 | Ring authorization rebuilt per D9: model proposes one exact catalog tool + args; deterministic host authorizes by annotation- and profile-derived risk (execute / hold-for-exact-approval / fail-closed). ACCEPTANCE: no positive-intent regex authorization; no peer/tool-name special cases; no second language classifier; generic annotation+profile policy; unknown risk fails closed. Delete the obsoleted phrase-family branches. | BUILD; PORT the typed TurnPlan (merged RIV-1) and existing standing-approval resolution where they fit | `packages/core/src/taskingPolicy.ts` + per-connection trusted-profile config surface | 150 min | [#1006](https://github.com/zenod-ai/zenod/issues/1006) | Ticket worker | `/root/mc_2_memory_token` | Done / integrated | [#1021](https://github.com/zenod-ai/zenod/pull/1021) / `codex/mc-14-host-authorization` | `7c11460d3c8be04c6a99c6fd2dd52caa7bded444` | Independent review + CI green; squash merge `cff97ae` | 2026-07-29 20:48 CEST | Exact connection+tool+args approval, generic annotation/profile risk, whole-step zero-call multi-proposal barrier, fail-closed unknown. |
| MC-15 | 3 | Ring outcome algebra (D15): every turn renders exactly one of { answer · clarification · held draft + approval question · verified success receipt · honest failure/unknown }. Mutation outcomes are host-rendered from the verified receipt — Ring writes no freeform mutation prose; NO RECEIPT → no "saved"/"sent"/"posted"/permalink, ever. Read-only answers remain natural model prose. No raw guard diagnostics or duplicate blocks. | BUILD; PORT salvageable pieces of `replyGate.ts` | `packages/core/src/replyGate.ts` + Ring render call sites | 90 min | [#1009](https://github.com/zenod-ai/zenod/issues/1009) | Ticket worker | `/root/mc_3_stt_fail_loud` | Done / integrated | [#1019](https://github.com/zenod-ai/zenod/pull/1019) / `codex/mc-15-outcome-algebra` | `7c11460d3c8be04c6a99c6fd2dd52caa7bded444` | Independent review + CI green; squash merge `d847103` | 2026-07-29 21:16 CEST | Integrated; the five-outcome algebra and receipt-only mutation rendering are on `main`. |
| MC-16 | 3 | Ring catalog fidelity — the heart of the Ring, as a first-class acceptance package: per connected MCP and per tenant, (1) authenticated `tools/list` runs; (2) the UI shows the actually discovered tools; (3) Ring receives exact names, descriptions, schemas, annotations; (4) Ring can only propose tools present in that catalog; (5) refresh detects added/removed/changed tools; (6) Ring never invents `ask_<name>` or fallback capabilities; (7) skills remain advisory and never replace discovery; (8) receipt claims come only from the tool actually invoked; (9) tenant A's catalog, credentials, and receipts never reach tenant B. Write these as tests first; fix the gaps they find. | PORT/verify: r-h1 generic MCP discovery, catalog-intent work, and the unique-tool-name fix (`fd76aef`) already on main — test them, do not rebuild them | Ring catalog/peer surfaces (`peerCatalog.ts`, `peerClient.ts`, `meshGateway.ts`) + tests | 120 min | [#1008](https://github.com/zenod-ai/zenod/issues/1008) | Ticket worker | delivery manager `/root` | Done / integrated | [#1018](https://github.com/zenod-ai/zenod/pull/1018) / `codex/mc-16-catalog-fidelity` | `7c11460d3c8be04c6a99c6fd2dd52caa7bded444` | Independent review + CI green; squash merge `4beccb8` | 2026-07-29 20:10 CEST | Test-only nine-check acceptance package proves integrated r-h1/catalog-intent/`fd76aef`; no production gap reproduced. |

Mint one GitHub issue per ticket at dispatch; each issue carries its Ledger row verbatim plus acceptance bullets from this spine.

## Branch And Integration

- Base pinned at `0fac451`. Wave 1 branches: `codex/mc-<n>-<slug>` from the pin, one worktree each.
- Wave 1 integrates to `main` PR-by-PR (MC-1 first — it is a no-op by D8 and unblocks wave 2's surfaces). After wave 1: re-pin, journal the new SHA ("Wave 2 base: <sha> — integrated wave 1, pinned").
- MC-5 and MC-1 share no window: MC-5 starts only from the wave-2 pin. Full test suites run at most once per pinned base; never re-run for docs-only movement.
- Deploys ride the existing CI (GHCR fleet rolls forward on publish — `74ac96a`'s behavior). The journey is walked on the exact deployed SHA, recorded per lap.

## Human Gates

| Gate | Owner | Trigger | Exact input |
|---|---|---|---|
| G1 — SHIP-A approval | Jordi | Delivery manager posts the SHIP-A test package (steps 1–10, screenshots, deployed SHA) | Jordi walks the capture journey, sends his two real pending voice notes, confirms both landed as commits with receipts. Reply "SHIP-A approved" or defects. |
| G1b — SHIP-B approval | Jordi | Delivery manager posts the SHIP-B test package (steps 1–8, screenshots, deployed SHA) | Jordi walks the assistant journey (reply-by-voice, held draft, approval, publish receipt, catalog fidelity, capture independence). Reply "SHIP-B approved" or defects. |
| G2 — Defaults beyond tenant zero | Jordi | Any proposal to change binding defaults for tenants other than tenant zero | Explicit "yes" per default change. |

Blocked protocol: a worker blocked on a human decision posts, as its ENTIRE next message, `BLOCKED ON JORDI: <one question + options + recommendation>` and that thread stops. Polishing adjacent work while parked at a gate is a named defect.

## Recovery And Takeover

- Heartbeat: every 30 minutes, one line — `lap/state | blocker | ETA`. Two consecutive ETA slips: stop and report options.
- The manager reassigns any ticket silent past its budget.
- A successor steward bootstraps from: this spine → Issue Ledger issues → `git log` since the pin → the journal below. The evidence folder holds the why.

## Validation Evidence

(append per lap: date, deployed SHA, step reached, breakage, fix PR)

- 2026-07-29 · preflight, `d847103`: deployment blocked before SHIP-A step 1 because publish run [30483664117](https://github.com/zenod-ai/zenod/actions/runs/30483664117) crashed importing AJV from core. Exact fix [#1023](https://github.com/zenod-ai/zenod/pull/1023) independently reviewed and CI-green; squash merge `4c1ca5e`; main runtime-image boot smoke and GHCR push passed in [30484461054](https://github.com/zenod-ai/zenod/actions/runs/30484461054).
- 2026-07-29 · deployment preflight, `1a9fd83f96d1edb777b7df8a67eddee221d44516`: final publish run [30486087626](https://github.com/zenod-ai/zenod/actions/runs/30486087626) passed runtime boot and pushed `ghcr.io/zenod-ai/zenod:sha-1a9fd83`; Zenod, Ring, and Phylax desired/runtime images and public `/api/health.sha` all resolve to the exact commit, all services are 1/1, `/healthz` is green, MCP rejects unauthenticated access, and Phylax reports WhatsApp connected with receive path ready and no restart required. A first Phylax gate inspected watchdog fields on generic `/healthz`, cleanly rolled back, then redeployed the candidate; corrected verification on `/api/health` passed.
- 2026-07-29 · scoped credential/config preflight, `1a9fd83f96d1edb777b7df8a67eddee221d44516`: rotated every peer credential exposed during private diagnostics. Zenod `memory-channel` `tools/list` is exactly `ask_brain`, `chat_with_zenod`, `get_task_result`, `ingest_memory`, `search_memory`, `store_memory`; Ring `capture-ticket` is exactly `record_capture_ticket`. Phylax persists canonical bearer URLs for both authorities, discovers the exact six Zenod tools, and has `voiceDefault=capture`. Ring's three canonical bearer peers are `connected/ready`; Zenod is private/tenant/no-financial, Callisthenes external/tenant/no-financial, and Phylax private/tenant/no-financial, all with trusted MCP annotations enabled.
- 2026-07-30 · D19 live regression, `080f985b0e7a4638f65f4b5a443d7d51fa823fa6`: the answer pinned to `Log/2026-07-29.md#^e-a7f53e` rendered all four questions intact, including “show the saved pages”. PR #1028 removed the lexical sanitizer and typed the MCP answer envelope; PR #1029 made the outcome boundary consume that exact typed structure. The fix diff adds no `RegExp`, `match`, `test`, or `replace` policy pattern.
- 2026-07-30 · SHIP-A clean lap, `080f985b0e7a4638f65f4b5a443d7d51fa823fa6`: steps 1–10 passed uninterrupted. A fresh 24:14 voice note received immediate progress, successful STT, one durable idempotent job and one terminal receipt; the persisted job survived a forced Phylax restart; the opened commit showed `verbatim: yes` at `Log/2026-07-29.md#^e-3a77f3`; the receipt reply returned cited open questions; the channel credential listed exactly six memory tools and no action tools; a killed WhatsApp socket recovered from generation 1 through degraded generation 2 to ready generation 3 in about three seconds; a fresh 24-second note landed with a receipt; and a 15-minute quiet window left generation 3 unchanged with no restart. Evidence: `docs/evidence/mechanical-capture-ship-a-2026-07-30/`.
- 2026-07-30 · normal-operation clean lap, `bc218baa74c37ee63c03948a8349b7694381365a`: a fresh 27-second WhatsApp voice note received immediate progress and terminal receipt `3be6837a9c1464f0c5959d32f9a1825ce2e5a6a7` at `Log/2026-07-30.md#^e-a96277`. Two natural follow-ups returned “Silver Orchard 4,” “verify the capture ledger on Tuesday,” and then “Tuesday,” both citing that exact evidence. Each answer rendered one host-owned read-only status. The Phylax outbox was terminal/delivered with `last_error=null`, Ring held the matching provider-message ticket, and the brain HEAD changed exactly once for capture and not for either read. Evidence: `docs/evidence/mechanical-capture-conversation-repair-2026-07-30/`.

## Handoff Journal

- 2026-07-29 · planner (v2.2 — design frozen): MC-11 regression and D19 supersede every lexical read-only guard. `ask_brain` returns typed `answer_content`; the host appends typed `read_only_status`; mutation wording renders only from a verified `mutation_receipt`, which this lane cannot produce. The exact acceptance regression cites `Log/2026-07-29.md#^e-a7f53e` and must preserve the word “saved” intact. No output scan/strip/replace is policy. SHIP-A is the only active scope; SHIP-B, MC-9, and any additional Wave-3 work are parked until Jordi G1.
- 2026-07-29 · planner: Spine authored. Supersedes RIV for execution (D12); parent stabilization spine set ON HOLD in place. Base pinned `0fac451`. Grounding: incident root-cause doc, counter-proposal, and canonical sink-design doc in `docs/evidence/whatsapp-zenod-stabilization-2026-07-29/`. Jordi's standing constraint recorded as D7 (multitenant, no hacks). Dispatch prompt in Appendix A.
- 2026-07-29 · delivery manager `/root`: Bound as epic worker and active steward. Read the spine and canonical sink-design document end to end. Minted issues MC-1 [#997](https://github.com/zenod-ai/zenod/issues/997), MC-2 [#994](https://github.com/zenod-ai/zenod/issues/994), MC-3 [#1001](https://github.com/zenod-ai/zenod/issues/1001), MC-4 [#995](https://github.com/zenod-ai/zenod/issues/995), MC-5 [#996](https://github.com/zenod-ai/zenod/issues/996), MC-6 [#999](https://github.com/zenod-ai/zenod/issues/999), MC-7 [#993](https://github.com/zenod-ai/zenod/issues/993), MC-8 [#1000](https://github.com/zenod-ai/zenod/issues/1000), and MC-9 [#998](https://github.com/zenod-ai/zenod/issues/998). Collaboration capacity permits three ticket workers alongside the steward; dispatched MC-1/2/3 at 17:23 CEST and queued MC-4 for the first released slot. This is a scheduling-only exception: the Wave 1 pin, worktree-first law, issue scope, budgets, and integration order are unchanged.

- 2026-07-29 · planner (amendment v1.1, applied while Wave 1 active): External design review accepted on four points. (1) Exactly-once moved to Zenod ownership — new MC-10 (`idempotencyKey` + durable unique index), D14 rewritten; Phylax-side dedupe alone cannot close the crash-between-accept-and-record window. (2) Evidence-scoped follow-ups — new MC-11 (`ask_brain` optional `contextRefs`), MC-5 records the last capture's `evidenceRef`. (3) Linguistic action-refusal REMOVED as promised behavior — it would smuggle intent detection back in; replaced by capability-only safety + optional deterministic footer (new D15); MC-7 and SHIP amended accordingly. Honest consequence journaled: voice always stores in era 1, including "don't store this" voice notes. (4) D6 watchdog rewritten to transport-lifecycle + keepalive signals; user-message recency rejected as a health signal (restart-loop trap during quiet hours); MC-4 row amended BEFORE its dispatch. Also: D1 clarified (Zenod librarian model calls are legitimate), new D16 (structured validated arg mappings), SHIP re-cut to 10 steps including restart-mid-job and idle-window laps. Wave 1 MC-1/2/3 scopes unchanged; steward must relay D16 to the active MC-1 worker (mapping schema is structured, not string templates) and mint MC-10/MC-11 issues.
- 2026-07-29 · planner (amendment v1.2): Second external review accepted on both remaining points. (1) D6/MC-4: use Baileys' native keepalive (configure + test, never duplicate); watchdog scope = bounded startup/handshake, native close events, worker/event-loop heartbeat; honesty clause added — keepalive proves transport only, an external canary (HARDEN) or real receipt is the only end-to-end inbound proof. (2) D15: explicit truth boundary — ask_brain output renders only as answer content, host-owned deterministic status appended, mutation-success language only from verified mutation receipts (the Q&A lane has none, structurally). Also: D16 split across tickets (MC-1 shape only / MC-5 dispatch-time validation / MC-6 save-time validation — settings-storage worker does NO MCP discovery); MC-10/MC-11 file surfaces widened to the job store and shared schema types. Steward: relay the D16 split to the active MC-1 worker and the D6 rewrite to MC-4 before it starts.
- 2026-07-29 · planner (v2.0 — direction restructure, accepted by Jordi): The era framing is RETIRED; this spine now builds the assistant with capture guaranteed underneath. Accepted synthesis (Jordi + external review): (1) D17 reply-to-receipt seam — Phylax routes by message type + WhatsApp reply-context (structural), so a text or VOICE reply to a capture receipt reaches Ring with the exact evidenceRef, and standalone voice always captures; this dissolves the voice-loop limitation without any intent classifier. (2) D18 Ring = delegator: asks when unclear, delegates to final agents, executes nothing itself, receives capture tickets (summary + evidenceRef) for context; receipts remain host-templated (incident lesson) but the user experiences one assistant. (3) D9 rewritten from moratorium to surgery: MC-14 retires phrase authorization for memory-scope tools (shrink-only, net-negative lines), MC-15 enforces single-outcome rendering; external actions keep held-draft + exact approval. (4) DoD split into SHIP-A (capture, unchanged 10 steps, gate G1 — the two real voice notes land here, not blocked by anything else) and SHIP-B (assistant journey, gate G1b). New tickets MC-12..MC-15 in wave 3; MC-7 rescoped (steward updates issue #993). Wave 1 and wave 2 tickets are UNCHANGED — nothing in flight is invalidated. Steward: mint MC-12..MC-15, update #993, continue wave 1.
- 2026-07-29 · planner (v2.1 — co-approver amendments, architecture approved): (1) D9 rewritten to "model proposes, host authorizes": deterministic risk policy from MCP annotations + per-connection trusted profiles (execute / hold-for-exact-approval / fail-closed on unknown) — no tool-name lists, no phrases, no second classifier; closes both failure directions (regex veto AND persuaded auto-execute). (2) MC-14 acceptance rewritten to the five correctness items; "net-negative lines" dropped as a criterion. (3) MC-15 formalized as the outcome algebra { answer · clarification · held draft+ask · verified receipt · honest failure }; mutation prose is host-rendered from receipts only. (4) D17 gains tenant `voiceDefault = capture | assistant` (default capture) — structural config; the stored-not-executed limitation of standalone spoken instructions stays visible and intentional. (5) New MC-16: Ring catalog fidelity as a first-class nine-point acceptance package (PORT/verify existing r-h1 discovery + `fd76aef`, test-first); SHIP-B gains the catalog spot-check step. Steward: mint MC-16 with the others; MC-14/15 must not dispatch from the pre-v2.1 wording.
- 2026-07-29 · delivery manager `/root` (v2.1 reconciliation/resume): Read the amended spine and v2.1 journal end to end before resuming work. Minted MC-10 [#1007](https://github.com/zenod-ai/zenod/issues/1007), MC-11 [#1004](https://github.com/zenod-ai/zenod/issues/1004), MC-12 [#1005](https://github.com/zenod-ai/zenod/issues/1005), MC-13 [#1003](https://github.com/zenod-ai/zenod/issues/1003), MC-14 [#1006](https://github.com/zenod-ai/zenod/issues/1006), MC-15 [#1009](https://github.com/zenod-ai/zenod/issues/1009), and MC-16 [#1008](https://github.com/zenod-ai/zenod/issues/1008). MC-14/#1006 contains only the five v2.1 correctness checks and explicitly rejects line-count criteria; MC-15/#1009 contains the five-outcome algebra and receipt-only mutation rendering; MC-16/#1008 contains the nine test-first catalog-fidelity checks and PORT/verify law for r-h1/catalog-intent/`fd76aef`. Replaced stale bodies for MC-1/#997, MC-4/#995, MC-5/#996, MC-6/#999, MC-7/#993, and MC-8/#1000. Resumed MC-1/2/3 from preserved worktrees at 18:20 CEST; MC-4 remains frozen until the MC-1 worker slot releases.
- 2026-07-29 · delivery manager `/root` (Wave 1 partial integration): Independently reviewed and re-ran MC-1 and MC-3 focused validation. GitHub shared identity prevented formal self-approval, so separate-agent review evidence was recorded in each PR. Required CI passed. Integrated MC-1 first via [#1002](https://github.com/zenod-ai/zenod/pull/1002) at squash merge `3423c1d`, then MC-3 via [#1010](https://github.com/zenod-ai/zenod/pull/1010) at `b911bf8`. Issues #997 and #1001 closed. MC-2 and MC-4 remain on the original Wave 1 pin as required; no rebase.
- 2026-07-29 · delivery manager `/root` (MC-2 D3 scope decision): The worker traced the real hosted path and proved `auth.ts`/`mcp.ts` alone only protect the legacy endpoint: the chassis has one replace-in-place token per tenant, no token profile/alias, and drops auth scope before MCP construction. The attempted blanket memory restriction for every hosted tenant token was rejected and reverted because it would break ordinary clients without creating the distinct channel credential D3 requires. Per D3's explicit catch-all, the steward authorized the smallest necessary file-surface widening: chassis token-profile/alias storage, authenticated-profile propagation, server wiring, issuance, and hosted-route tests. The ordinary full-surface tenant credential must remain unchanged. A live credential was exposed only inside a private worker transcript during read-only tracing; its value must not be copied into issues, code, tests, or evidence, and the affected credential must be rotated before live SHIP reliance.
- 2026-07-29 · delivery manager `/root` (MC-4 integration): Initial PR #1012 passed tests but independent review blocked a Compose restart-loop: a single probe could kill PID 1 during the gateway's normal two-second reconnect, and persistent auth-terminal states would loop forever. The worker added a stable outage clock and separated transient, sustained-restartable, and operator-action states. Independent re-review passed 41 focused tests; the worker's full 820-test suite and GitHub CI passed. Squash-merged MC-4 at `5c52c1b`.
- 2026-07-29 · delivery manager `/root` (MC-2 security review): The hosted alias implementation proved the exact-six MCP list/call surface, rotation, lifecycle inheritance, and tenant separation, but independent review reproduced a D3 escape: the profiled credential also authenticated a generic protected mutation API because route/UI auth ignored the credential profile. PR #1011 remains draft and blocked until every profiled credential is MCP-only. Manager review additionally rejected the earlier signed standalone token as an orphan duplicate with no production issuance or independent revocation; the smallest hosted mechanism must be the sole implementation.
- 2026-07-29 · delivery manager `/root` (Wave 1 complete / Wave 2 pin): MC-2 fixed both review blockers: profiled credentials are MCP-only across generic routes, UI/session, and OAuth issuance; the orphan signed standalone mechanism was removed. Independent re-review passed, focused 89+45 tests and typechecks passed, and CI passed. Squash-merged at `2ace3bd`. Wave 1 is complete. **Wave 2 base: `2ace3bd99d3e8810cce444669cfa3ca2df414bee` — integrated Wave 1, pinned.** Dispatching MC-5, MC-6, MC-7, and the sequential MC-10→MC-11 stream from this exact SHA.
- 2026-07-29 · delivery manager `/root` (MC-6 secure discovery scope): The worker proved the browser cannot call downstream `tools/list` safely: tenant settings expose only `downstreamTokenConfigured`, intentionally never the raw credential, and no authenticated catalog proxy exists. The steward authorized the smallest D3/D16-compatible widening: a tenant-authenticated server-side discovery endpoint in `phylaxUnit.ts`, a server-only per-tenant credential accessor only if required, and focused server tests. The response may contain only sanitized exact tool names, descriptions, input schemas, and annotations; no credential or secret-bearing downstream error may reach the browser. UI re-discovers immediately before PUT; MC-5 remains the dispatch-time validator.
- 2026-07-29 · Jordi (G2 approval): Approved Option A — D17 routing defaults apply globally to all tenants: standalone voice captures to `store_memory`, standalone media routes to `ingest_memory`, and ordinary text routes to Ring. The defaults remain per-tenant overridable; implementation contains no tenant-zero branch. MC-7 resumed from the unchanged Wave-2 pin.
- 2026-07-29 · delivery manager `/root` (MC-6/MC-7 integration): Independent review blocked MC-6 until save-time mapping validation checked live JSON-schema type, enum, and const compatibility; final re-review and CI passed, then [#1014](https://github.com/zenod-ai/zenod/pull/1014) squash-merged at `c264733`. Independent cross-PR review blocked MC-7's original media default because it mapped the textual handoff envelope to `bytesRef` and hardcoded `document`; the worker replaced that with structural `artifactUrl`, derived `mediaType`, and `filename` sources. Re-review and CI passed, then [#1015](https://github.com/zenod-ai/zenod/pull/1015) squash-merged at `4e99faf`. MC-5 remains gated on consuming those sources, the effective voice resolver, the receipt invitation, and terminal polling for both `store_memory` and `ingest_memory`.
- 2026-07-29 · delivery manager `/root` (MC-10 integration / MC-11 start): Independent review blocked MC-10 three times before acceptance: the gateway-advertised schema omitted `idempotencyKey` and queue claim was non-atomic; startup recovery could requeue a still-live rolling-overlap worker; shutdown could close SQLite under an active drain/heartbeat. The final implementation uses one canonical schema, atomic tenant-scoped owner claims with renewable leases, expired-claim recovery, stale-owner fencing, and awaited shutdown. Final re-review, 838 server tests, build/typecheck, and CI passed; [#1016](https://github.com/zenod-ai/zenod/pull/1016) squash-merged at `4cf5d64`. The sequential worker then created MC-11 worktree/branch from that exact integrated SHA and published test-first [#1017](https://github.com/zenod-ai/zenod/pull/1017).
- 2026-07-29 · delivery manager `/root` (MC-11 integration): Independent review blocked the first MC-11 head because multiple refs in one daily log overwrote each other, D15's read-only output truth boundary was not enforced, and tenant isolation lacked an explicit two-vault collision test. The final head accumulates same-path pinned spans, resolves colliding refs only in the current engine's vault, and rejects mutation-success language in core and again at the MCP host boundary with a deterministic read-only status. Independent re-review passed, core 523 passed/6 skipped, server 843 passed, and GitHub CI passed; [#1017](https://github.com/zenod-ai/zenod/pull/1017) squash-merged at `212c600`.
- 2026-07-29 · delivery manager `/root` (Wave 2 complete / Wave 3 pin): MC-5 independent review blocked two D13/D14 failure modes after its first green CI: transient poll transport failures were terminalizing durable captures, then an in-flight poll could overrun the four-minute foreground deadline. The final head retries transient failures without leaving `polling`, accepts only matching structured terminal results, races foreground polls against the exact remaining deadline, and hands stalled work to background/restart custody. Final independent review, 850 server tests, build/typecheck, and CI passed; [#1013](https://github.com/zenod-ai/zenod/pull/1013) squash-merged at `7c11460`. Wave 2 is complete. **Wave 3 base: `7c11460d3c8be04c6a99c6fd2dd52caa7bded444` — integrated Waves 1–2, pinned.** MC-12, MC-14, MC-15, and MC-16 dispatch immediately; MC-13 is assigned to the first released worker because the four-slot collaboration cap permits the steward plus three ticket workers.
- 2026-07-29 · delivery manager `/root` (MC-16 test-first PORT verification): Before implementation, recorded the exact inherited lineage on issue #1008: r-h1 `669292e` + `150a566`, live-schema tolerance `4d1ac76`, catalog-intent/locality `d6a10c6` + `5ddf505` + `28aaf83`, and unique-name repair `fd76aef`. Wrote all nine acceptance checks against the current production surfaces first. After correcting one invalid test fixture that used a non-standard annotation outside MCP's closed ToolAnnotations schema, every check passed without a production rewrite. Draft [#1018](https://github.com/zenod-ai/zenod/pull/1018) at `0d8c297` is intentionally test-only; focused core 92, full server 859, web 42, typechecks, builds, and diff check pass. Independent review and CI remain.
- 2026-07-29 · delivery manager `/root` (MC-15 handoff / MC-13 dispatch): MC-15 published draft [#1019](https://github.com/zenod-ai/zenod/pull/1019) at `a1886c5` from the exact Wave 3 pin. The implementation is confined to `replyGate.ts` plus tests and models the five v2.1 outcomes explicitly; local focused/core/server/workspace/script/schema gates pass. Per the journaled four-slot capacity queue, the released worker immediately dispatched MC-13 from the unchanged Wave 3 pin and must identify the Ring conversation-state surface before editing; MC-15 awaits independent review and CI.
- 2026-07-29 · delivery manager `/root` (MC-12 handoff / review rotation): MC-12 published draft [#1020](https://github.com/zenod-ai/zenod/pull/1020) at `69a1bb1` from the exact Wave 3 pin with 80 focused tests and full repository tests/build/typecheck/diff checks green; CI and independent review remain. The released MC-12 worker immediately began independent review of CI-green MC-16 at exact head `0d8c297`. MC-15 CI is also green and waits for the next review slot.
- 2026-07-29 · delivery manager `/root` (MC-16 integration): Independent review confirmed that all nine tests exercise real boundaries and re-ran 22 server, 42 web, and 94 core checks. The reviewer also confirmed each PORT lineage mapping, including equivalence of the named unmerged catalog-intent/locality refs to their merged commits. With CI green and no findings, [#1018](https://github.com/zenod-ai/zenod/pull/1018) squash-merged at `4beccb8`; issue #1008 closed. The same independent reviewer rotated to MC-15.
- 2026-07-29 · delivery manager `/root` (MC-14 handoff / MC-12 review): MC-14 published draft [#1021](https://github.com/zenod-ai/zenod/pull/1021) at `1614903` from the exact Wave 3 pin. Its generic host authorization consumes MCP annotations bounded by tenant-isolated trusted profiles, uses exact standing approvals, fails closed on unknown risk, and leaves capture plus `replyGate.ts` untouched. Focused core/server/web tests, typechecks/builds, changed-file lint, and diff check pass; CI and independent review remain. The released MC-14 worker rotated to independent review of CI-green MC-12.
- 2026-07-29 · delivery manager `/root` (MC-15 review block / MC-13 handoff): Independent review blocked MC-15 at `a1886c5` on four P1 truth-boundary gaps: approval-required results containing receipt-like IDs could become success, mutation actions outside the narrow legacy name set bypassed the algebra, model mutation prose streamed to web before final interception, and bare mutation URLs bypassed the no-receipt guard. The MC-15 worker resumed for narrow fixes and refreshed tests/CI. Separately, MC-13 published draft [#1022](https://github.com/zenod-ai/zenod/pull/1022) at `5cbfa28`: the pre-edit Ring state surface was recorded, the implementation is Ring-only with literal terminal-state gating and atomic tenant-local dedupe, and full local gates pass; CI and independent review remain.
- 2026-07-29 · delivery manager `/root` (MC-12 review block): Independent review blocked MC-12 at `69a1bb1` on one D17 crash window: provider send can complete before the receipt message ID is indexed to its evidence ref; a crash there leaves a visibly sent receipt permanently unknown because restart recovery resumes only polling captures. Existing tests restart only after index persistence. The author is queued to add restart-safe provider-outbound custody/reconciliation plus an exact crash-after-send-before-index regression, then rerun CI and independent review. All other D17 structural routing, voice context, tenant isolation, and media behavior passed review.
- 2026-07-29 · delivery manager `/root` (MC-13/MC-14 review blocks): MC-13's Ring store and consumer passed, but review proved the new `record_capture_ticket` intake was orphaned: no verified production capture-terminal producer calls it, so Ring receives zero tickets outside tests and caller-supplied `done` is not authoritative. The author is queued to add real durable producer wiring and an end-to-end terminal/retry/restart proof without touching `phylaxChannels.ts`. MC-14 review found two D9 violations: standing approval used subset args and ignored exact tool identity while retaining a forbidden name/description regex, and a two-tool model batch could execute the first side effect before rejecting the second. The author is queued to enforce exact connection+tool+args and fail the entire multi-proposal batch before any call. Both stale heads have green CI but require refreshed gates and re-review.
- 2026-07-29 · delivery manager `/root` (Wave 3 corrected heads): MC-12 pushed `5757635` with restart recovery for the provider-send/index crash window; MC-14 pushed `d12eebe` with exact connection+tool+canonical full-args approval, deletion of regex authority, and a pre-execution multi-proposal batch barrier; MC-15 pushed `97b05b6` with approval-before-receipt precedence, structural coverage of all mutation actions, pre-gate mutation-delta buffering, and a structural X/Twitter status-permalink guard that preserves ordinary read links. All three corrected heads passed their full local gates; refreshed CI and independent re-reviews are active/queued. The MC-15 author resumed MC-13 producer wiring.
- 2026-07-29 · delivery manager `/root` (MC-12/MC-14 second re-review blocks): MC-12 re-review proved `5757635` repaired only audit→index recovery, not the actual provider-send→audit crash. Background recovery could index the earlier pending receipt ID as terminal, while foreground/recovery could resend after a successful but unaudited provider call. The author resumed for a durable outbox/preallocated-ID or equivalent exact-boundary fix and crash regression. MC-14 re-review closed exact approval but proved the microtask batch barrier unsafe for production `streamText`: staggered tool-call chunks can let proposal A execute before B registers. The author resumed for a real model-step barrier (or structurally delayed connected execution) plus a staggered zero-upstream-call regression. Both stale heads have green CI and require refreshed gates. MC-15 corrected head `97b05b6` has green CI and awaits review.
- 2026-07-29 · delivery manager `/root` (MC-14 whole-step correction): MC-14 replaced the invalid microtask barrier and pushed `b4e9ffa`. Connected-MCP turns with live callbacks now structurally avoid `streamText`; `generateText` parses the whole model step and each tool's host preflight registers the complete proposal batch before any execution. A production-like staggered-chunk regression proves no executor entry, and distinct proposals produce zero upstream calls while identical retries dedupe. Full repository/workspace gates pass; fresh CI and independent re-review remain. The MC-14 author rotated to MC-15 re-review.
- 2026-07-29 · delivery manager `/root` (MC-13 producer correction): MC-13 pushed `c912b45`, replacing the orphan intake with a durable Phylax outbox producer and accepted-job journal recovery in permitted runtime/unit surfaces. Foreground/background terminal wakes and restart recovery emit canonical capture/job correlation; Ring ignores caller terminal/summary/evidence claims, re-reads the exact tenant-configured memory job through the scoped credential, and derives summary+evidence only from verified `done`. End-to-end tests cover nonterminal suppression, forged claims, retry/restart dedupe, and tenant isolation. Full gates pass; fresh CI and independent re-review remain.
- 2026-07-29 · delivery manager `/root` (MC-14 integration): Independent re-review passed `b4e9ffa` after verifying the full-step ordering against the installed AI SDK source: every connected-tool approval hook sees the complete proposal batch before any executor runs. Exact standing approval binds the opaque connection identity, exact tool, and recursively canonical full args; endpoint/token replacement invalidates it. No tool/peer-name, description, positive-intent regex, or second classifier grants authority; unknown risk fails closed and per-tenant profiles remain isolated. Focused re-review, full local gates, and refreshed CI passed; [#1021](https://github.com/zenod-ai/zenod/pull/1021) squash-merged at `cff97ae`.
- 2026-07-29 · delivery manager `/root` (MC-13/MC-15 residual re-review blocks): MC-15 re-review closed its four original P1s but found the X/Twitter status guard overbroad: it blocked an exact URL grounded by a same-turn authoritative read and then appended that same evidence, producing a contradictory failure. The author resumed for an exact-evidence exception while keeping unmatched/zero-tool status links blocked. MC-13 re-review closed its orphan/authority P1 but found dedupe keyed only by tenant+captureId, allowing WhatsApp `42` to suppress distinct Telegram `42`. The author is queued to carry one canonical tenant+channel/provider namespace (+conversation/chat where needed) through producer outbox, Ring store migration, and tests.
- 2026-07-29 · delivery manager `/root` (MC-12/MC-15 final corrected heads): MC-12 pushed `1d8edaf` after confirming Baileys officially accepts a caller-supplied message ID. A durable tenant-scoped outbound intent now preallocates and persists that provider ID before send, replays the same ID on restart, and marks terminal receipt eligibility so earlier progress IDs cannot be indexed as terminal; exact crash-boundary tests and full gates pass. MC-15 pushed `299a0ed`: X/Twitter status links escape the no-receipt guard only when every normalized URL appears in successful same-turn declared/non-mutating read evidence and the draft passes safe synthesis; zero-tool, mismatched, raw-envelope, and mutation cases remain blocked. Both await refreshed CI and final independent re-review.
- 2026-07-29 · delivery manager `/root` (MC-12 residual block / MC-15 review pass): MC-12 re-review confirms the provider-success→audit window is closed but blocks `1d8edaf` on a distinct persisted terminal-before-intent restart state: recovery sees the done job but cannot reconstruct delivery, so the receipt can strand with no provider side effect. The author is queued to persist intent before terminal commit or safely recover this exact state with same-ID/no-duplicate proof. MC-15 independent final re-review passed exact `299a0ed`, including the evidence-grounded status-URL exception and all original D15 gaps. GitHub has no check for that rewritten head, and the branch conflicts with integrated MC-14; integration must preserve history (no rebase), resolve narrowly, then refresh review and CI.
- 2026-07-29 · delivery manager `/root` (MC-15 no-rebase integration refresh): Merged current `main` (`cff97ae`, including MC-14) into MC-15 without rebasing. The sole conflict was the connected-tool execution block in `aisdk.ts`; the resolution preserves MC-14's whole-step host authorization and MC-15's annotation/tool-kind-derived mutation-attempt evidence for the outcome algebra. Exact head `d90980fc530ffb5ae2d62568df50763067e86156` passes 85 focused D9/D15 tests, core typecheck, full build, core 510/6 skipped, chassis 89, server 860, web 42, scripts 194, schemas 27, and diff check. Fresh CI is queued and an independent post-integration review is active.
- 2026-07-29 · delivery manager `/root` (MC-12/MC-13 final correction heads): MC-12 pushed `8c27b09c4bb192a064e5776765357b3d7c206488`; its exact seeded done+evidence+delivered_at/no-intent restart regression proves recovery creates a durable tenant-scoped intent, sends once, reuses the same provider ID after a provider-success/before-audit crash, eventually records messageId→evidenceRef, rejects the wrong tenant, and sends no third time. MC-13 pushed `d202d0203dd19712bb4827f3ba2af4f40ed0992c`; canonical `(tenantId, surface, conversationKey, providerMessageId)` identity now spans the Phylax outbox and Ring state with in-place legacy migrations, while tests cover cross-channel, same-channel/different-chat, retry/restart, and tenant isolation. Both full local gates pass; fresh CI and independent cross-review are active.
- 2026-07-29 · delivery manager `/root` (MC-12 integration): Independent review passed exact `8c27b09` after re-running 95 focused Phylax/WhatsApp/Telegram tests and proving the seeded terminal-before-intent restart state, same-ID provider replay, eventual evidence mapping, wrong-tenant rejection, and no third send. Fresh CI passed; [#1020](https://github.com/zenod-ai/zenod/pull/1020) squash-merged at `c288bec`. The shared clone fast-forwarded on `main` without touching user-owned untracked evidence.
- 2026-07-29 · delivery manager `/root` (MC-13 authority block / MC-15 composition correction): MC-13's canonical identity and migrations passed, but production review blocked `d202d02`: the Phylax outbox sends Ring's `record_capture_ticket` through the Zenod capture credential, and Ring accepts its local queue or any generic `get_task_result` peer instead of the originating tenant memory authority. The author resumed for a distinct per-tenant post-terminal Ring route plus exact Zenod-authority verification; Ring remains absent from pre-terminal capture. MC-15 review found its D9 conflict return preceded D15 mutation classification, misrendering rejected writes as failed reads. Exact `1c0b8bf57b6bd23d2cef05ef3450ed09baa954c2` moves mutation metadata before that return and adds a real AI-SDK→engine→reply-gate regression proving zero upstream calls and one honest mutation failure. Full local gates pass; refreshed CI and re-review are active.
- 2026-07-29 · delivery manager `/root` (MC-15 integration): Independent re-review passed exact `1c0b8bf` after confirming every D9 multi-proposal conflict carries D15 mutation metadata and the new AI-SDK→engine→reply-gate regression makes zero upstream calls, suppresses raw conflict/read-failure prose, and renders one honest mutation failure. The reviewer ran 228 focused checks and three typechecks; full local gates and fresh CI passed. [#1019](https://github.com/zenod-ai/zenod/pull/1019) squash-merged at `d847103`; the shared clone fast-forwarded on `main`.
- 2026-07-29 · delivery manager `/root` (MC-8 publish preflight): The first deployment breakage reproduced on main publish run [30483664117](https://github.com/zenod-ai/zenod/actions/runs/30483664117): the runtime image crashed with `ERR_MODULE_NOT_FOUND` for AJV imported by core. The lockfile correctly installs AJV 8 in the core workspace after production prune, but the Docker runtime stage copied only root and server dependency trees. One-line [#1023](https://github.com/zenod-ai/zenod/pull/1023) copies `packages/core/node_modules`; exact `bd38af0` passed fresh install/build/prune/module-import checks, independent review, and CI, then squash-merged at `4c1ca5e`. Main publish run [30484461054](https://github.com/zenod-ai/zenod/actions/runs/30484461054) passed the definitive runtime boot smoke and pushed `sha-4c1ca5e`. No SHIP step is claimed until the final Wave-3 SHA is deployed and the live walk starts from step 1.
- 2026-07-29 · delivery manager `/root` (MC-13 post-main re-review block): The worker fixed both earlier P1s and merged exact main `4c1ca5e` without rebasing into candidate `cc89fac`; distinct per-tenant least-privilege Ring delivery and exact originating Zenod authority verification now pass, frozen surfaces remain untouched, full local gates pass, and fresh CI is green. Independent re-review found one new production regression in the merged runtime: both provider-send branches return before `wakeCaptureTickets`, so a capture that becomes terminal in background cannot deliver its Ring context until restart. The producer-level test masked this with manual terminal calls. The author resumed for an await-then-wake correction plus an actual background-terminal runtime regression; `cc89fac` must not merge.
- 2026-07-29 · delivery manager `/root` (MC-13 integration / Wave 3 complete): Final exact `b62f21b` awaits either provider send, wakes ticket recovery only after successful WhatsApp or Telegram delivery, and returns the original provider result. Its replacement production regression traverses `createPhylaxUnit` → organ foreground deadline → actual background terminal poll → runtime provider callback → journal recovery → distinct Ring route, with no restart or direct producer manipulation; it proves one context ticket, stored receipt IDs, and no duplicate resend for both channels. The two earlier P1s remain closed: a distinct per-tenant `capture-ticket` credential exposes only `record_capture_ticket`, and Ring accepts terminal evidence only from the exact originating Zenod URL+token authority, capture tool, job ID, and kind while rejecting local/generic substitutes. Independent final review, full local gates, and fresh CI passed; [#1022](https://github.com/zenod-ai/zenod/pull/1022) squash-merged at `1a9fd83`. Wave 3 is code-complete; final publish run [30486087626](https://github.com/zenod-ai/zenod/actions/runs/30486087626) is active.
- 2026-07-29 · delivery manager `/root` (final deployment): Publish [30486087626](https://github.com/zenod-ai/zenod/actions/runs/30486087626) passed runtime boot and pushed exact `sha-1a9fd83`. Dokploy's application desired images were updated, but its deploy/redeploy actions created no deployment and changed no service; the steward therefore used the smallest reversible in-scope fallback, updating only the exact three Docker services to the already-published digest while preserving volumes and environment. Zenod, Ring, and Phylax converged 1/1 on the exact full SHA and all public health/auth probes passed. A false Phylax gate read detailed watchdog fields from generic `/healthz`, causing a clean rollback to the prior image; after correcting the gate to inspect `/api/health`, the final candidate redeployed and proved WhatsApp connected, receive path ready, no outage, and no restart required.
- 2026-07-29 · delivery manager `/root` (G2 defaults + security/config preflight): Jordi approved the global D17 defaults. The steward rotated all credentials exposed during private diagnostic output before SHIP, including both replaceable scoped credentials and the affected Callisthenes/Phylax primary credentials. Ring's wallet now uses canonical `/mcp` bearer endpoints with explicit trusted profiles; its Zenod catalog is the exact six-tool memory scope, Callisthenes and Phylax catalogs are authenticated/ready, and no wallet URL embeds a token. Phylax now points standalone capture directly to the same Zenod memory authority held by Ring, holds the distinct one-tool Ring capture-ticket credential, persists `voiceDefault=capture`, and re-discovers the exact memory catalog. SHIP-A starts from step 1 on `1a9fd83`.
- 2026-07-30 · delivery manager `/root` (v2.2 / D19 correction): Fresh worktree PR [#1028](https://github.com/zenod-ai/zenod/pull/1028) removed `sanitizeReadOnlyAnswerText` and returned typed `answer_content` with a typed host-owned read-only status. The first live probe proved the correct model answer was still discarded by the frozen outcome boundary; Jordi explicitly approved that exact boundary fix. Fresh worktree PR [#1029](https://github.com/zenod-ai/zenod/pull/1029) made `replyGate` consume only the exact typed envelope and render it structurally, without inspecting answer words. The single independent D15/D19 review named one wrong-evidence regression reference; it was corrected to `^e-a7f53e`. Focused and full gates passed; one unrelated existing Ring capture-ticket timeout passed on exact rerun. Publish run `30498527169` passed, and Zenod, Ring, and Phylax deployed exact `080f985b0e7a4638f65f4b5a443d7d51fa823fa6`. The live probe preserved “show the saved pages” intact.
- 2026-07-30 · delivery manager `/root` (Ring terminal-capture conversation repair): The real post-SHIP-A replay exposed two sequential defects. First, Ring selected a stale voice-note memory and then polled Zenod's generic REST task endpoint with an MCP-only token, producing repeated 401s and a visible timeout even though the unintended job completed. Fresh-worktree [#1030](https://github.com/zenod-ai/zenod/pull/1030) loaded typed terminal capture tickets, excluded their legacy assistant prose structurally, polled memory jobs through scoped MCP `get_task_result`, and rendered store success only from an exact correlated terminal receipt. The first deployed replay then proved the model had produced the correct `^e-1d0d28` answer but the lexical reply gate discarded the word “stored.” Fresh-worktree [#1031](https://github.com/zenod-ai/zenod/pull/1031) left frozen `taskingPolicy.ts` and `replyGate.ts` untouched and rendered the no-action terminal-capture path as explicit typed `answer_content` plus host-owned `read_only_status`; no regex or output scan was added. Full local gates, CI retry, runtime boot smoke, and one independent D15/D19 review passed. Zenod, Ring, and Phylax deployed exact `c0cb89abd1d1fc43bb59beabaa8b52a0d081f2b8`. The live WhatsApp replay asked one clarification across the two existing capture tickets, then returned `Log/2026-07-30.md#^e-1d0d28`, its existing Inbox page, and `No action taken`; the brain remote HEAD remained `ee4f948`, proving no duplicate memory. Evidence is in `docs/evidence/mechanical-capture-conversation-repair-2026-07-30/`. The unintended stale £97 memory from the failed pre-fix lap remains preserved pending explicit cleanup authorization.
- 2026-07-30 · delivery manager `/root` (SHIP-A clean pass / G1 package): One uninterrupted steps 1–10 lap passed on deployed `080f985b0e7a4638f65f4b5a443d7d51fa823fa6`. The long capture produced exactly one job and receipt; job custody survived a forced Phylax restart; the evidence reply cited the same anchored note; the channel token exposed only the exact six memory tools; the receive watchdog recovered a killed Meta socket in about three seconds; a fresh short note then landed; and 15 minutes of ordinary silence caused no restart. Issue #1004 is closed with the D19 live evidence. Issue #1000 was reopened because PR #1023 had closed it mechanically; it now waits only for Jordi's G1 two-voice-note test. SHIP-B, MC-9, and any additional Wave-3 work remain parked.
- 2026-07-30 · delivery manager `/root` (normal WhatsApp operation stabilized): A live fresh-note lap first exposed stale-ticket selection; fresh-worktree [#1032](https://github.com/zenod-ai/zenod/pull/1032) made the newest terminal ticket typed `currentCapture` and instructed current/just-sent questions to use its exact evidence ref. The next lap captured and recalled `^e-9ce34e` correctly, then exposed a duplicate read-only footer on the second conversational turn. Fresh-worktree [#1033](https://github.com/zenod-ai/zenod/pull/1033) consumes typed `answer_content` before generic full-result preservation and forwards only answer text plus sources, leaving `read_only_status` host-owned. No output scan, strip, replacement, regex, or frozen-surface change was added. Focused regression, server 880, core 519/6 skipped, typecheck, CI, publish smoke, and the single independent D15/D19 review passed. Zenod, Ring, and Phylax deployed exact `bc218baa74c37ee63c03948a8349b7694381365a`; the final fresh voice note produced one terminal memory receipt at `^e-a96277`, and two natural cited turns rendered correctly with one status each and no read-side memory commit. Issue #1000 remains open only for Jordi's two-real-voice-note G1 walk; everything else remains parked.
- 2026-07-30 · delivery manager `/root` (grounded capture chronology / Makro defect closed): The natural “what was the voice note before that about?” interaction exposed a false “Makro supermarket” answer despite a correct capture ledger. Fresh-worktree PRs [#1034](https://github.com/zenod-ai/zenod/pull/1034)–[#1036](https://github.com/zenod-ai/zenod/pull/1036) first preserved typed host status and sources across conversation history without model-visible host prose. [#1037](https://github.com/zenod-ai/zenod/pull/1037) made previous-capture chronology an explicit typed relationship; [#1038](https://github.com/zenod-ai/zenod/pull/1038) required multi-capture questions to use one read-only memory call over exact refs instead of filing metadata; and [#1039](https://github.com/zenod-ai/zenod/pull/1039) prevented current-capture questions from rediscovering context through recent channel history or elapsed-time heuristics. The next deployed lap located the exact remaining defect: `ask_brain` could re-read a pinned ref's whole daily log and answer from another entry. Fresh-worktree [#1040](https://github.com/zenod-ai/zenod/pull/1040) mechanically confines that re-read to the supplied anchored span while leaving normal reads of other paths intact. Each exact fix passed its focused regression, Core (522 passed / 6 skipped), Server (880 passed), full build, a net-free lexical-policy diff check, fresh CI/runtime publish, and the single independent D15/D19 review. Zenod, Ring, and Phylax deployed exact `59732ddae8954b8c39678f35980d590bbca3687a`. One uninterrupted Q1–Q3 WhatsApp lap returned `^e-3ffa72` (Intermarché), then `^e-23ece7` (Conflent), then accurate one-line summaries for the five newest exact refs; it contained no Makro/property content, rendered one host-owned read status per answer, made one `ask_brain` call per question, and left brain HEAD `c85378b6483c5f3b8ff8b4fd12974e3017e888eb` unchanged. Screenshot: `docs/evidence/mechanical-capture-conversation-repair-2026-07-30/final-whatsapp-read-lap.jpg`. Issue #1000 remains open only for Jordi's two-real-voice-note G1 walk; SHIP-B and all other work remain parked.

## Open Questions

None permitted. Everything a worker could ask is answered in Decisions; anything else follows the catch-all.

## Appendix A — Dispatch prompt (paste-ready, versioned here)

```
You are the delivery manager for docs/EPIC-MECHANICAL-CAPTURE.md in zenod-ai/zenod.
Bind to that spine now and read it fully before any action; it is authoritative for
mission, decisions (D1–D14), waves, budgets, and gates. The canonical architecture
picture is docs/evidence/whatsapp-zenod-stabilization-2026-07-29/phylax-mcp-sink-and-experience-design.html.

Mission, one paragraph: make WhatsApp voice-note → Zenod memory mechanical and
guaranteed via a per-tenant binding table in Phylax (turn type → MCP tool), with a
memory-scoped token, fail-loud STT, a receive-path watchdog, terminal-receipt
replies, and zero Ring involvement on the capture path. Everything ships
multitenant-clean per D7: per-tenant config with defaults, no tenant-zero hacks.

Operating rules (violations are defects):
- Mint one GitHub issue per MC ticket from the Issue Ledger, then dispatch wave 1
  (MC-1 ∥ MC-2 ∥ MC-3 ∥ MC-4) as parallel workers.
- Every worker's FIRST action: git worktree add ../wt-<ticket> -b codex/mc-<n>-<slug> 0fac451.
  Never checkout/switch in a shared clone. Wave 2 re-pins after wave 1 integrates.
- PORT/DUPLICATE markings are law: MC-3 harvests fix/whatsapp-fail-loud and
  fix/drop-whisper-compile; MC-4 harvests the liveness signal from
  codex/phylax-reconnect-open-watchdog (review-blocked: evidence, not approved code);
  MC-5 duplicates the W-P4 coalescing pattern. Scratch-writing these fails review.
- Reject on sight: any PR importing taskingPolicy into the capture path (D1), any
  Zenod peer client (D2), any full-surface token on a channel binding (D3), any
  tenant-zero special case in code (D7). taskingPolicy.ts and replyGate.ts are
  frozen (D9).
- Heartbeat every 30 minutes: lap/state | blocker | ETA. Ticket budgets are in the
  Ledger; reassign anything silent past budget.
- Blocked on a human decision → your ENTIRE next message is
  "BLOCKED ON JORDI: <question + options + recommendation>" and you stop that thread.
- The SHIP journey (DoD steps 1–8) is YOURS personally: walk it on the live
  deployment in a loop — walk → first breakage → fix exactly that → deploy → walk
  again FROM STEP 1 — until one uninterrupted clean pass with a screenshot per step.
  Then hand Jordi the test package: "I walked steps 1–8 on <SHA>. Now you test."
  Never ask Jordi to click anything you have not clicked on that same build.
- Anything the spine leaves unanswered: simplest option, journal it, keep moving.

Go.
```
