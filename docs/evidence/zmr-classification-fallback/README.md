# Preserve successful classification when optional refinement fails

Issue #1196; worker `/root/zmr_8_chat_review`; base/deployed source `56c815f38aab6790b8afc165a8001e8fc0b5732b`. Branch `codex/zmr-classify-fallback`, isolated worktree `wt-zmr-classify-fallback`. Parent remains sole manager; no production mutation or credential access by this worker.

The operator correlated the latest failed filing job with one successful classify usage record followed by two failed calls. `classifyCandidates` first obtains a valid classification, then optionally expands the bounded candidate catalog for uncertain/new-page decisions. Its unguarded refinement call could throw away the good result, causing the engine to retry the entire classification and eventually label filing unavailable.

A deterministic regression reproduced that failure before the source change (five existing tests passed, new case failed with the refinement exception). The bounded repair retains the valid initial classification when optional candidate search/generation fails. It neither increases confidence nor changes dispositions/quotes/questions. Existing reconciliation against full-snapshot paths/titles/aliases still runs, preventing known duplicate pages. A successful refinement still replaces the initial result; an initial failure still propagates to the existing retry/Inbox behavior. A static warning records fallback failure without provider prose. Provider usage diagnostics remain unchanged.

Installed AI SDK `ai` 6.0.200 source explains the new safe diagnostic: `generate-object.ts` throws a cause-less `NoObjectGeneratedError` when `extractTextContent(result.content)` is undefined. JSON parse and schema-validation failures attach causes. For this object-classification call, observed `stop` plus no nested cause is consistent with absent final text, not evidence of a particular invalid schema field. The successful first call further argues against blindly relaxing the schema. No model/provider/schema changes are included; repeated initial empty outputs may still require separate diagnosis.

Validation: 16 focused meaning-note/diagnostic/schema checks PASS; two public engine store checks PASS (72 unrelated cases skipped), including successful filing with exactly two calls, preserved raw evidence/citation, and unchanged initial-failure Inbox fallback. All-workspace typecheck PASS. Tests use local repositories and scripted responses, not a live model. The fixture composer intentionally emits its standard cited “New fact recorded” text; the store regression verifies filing/source identity rather than asserting unimplemented semantic composition.

Live retest and required exact-head CI/review remain pending. Preserving a first result does not guarantee confident filing: its uncertainty remains authoritative, and lexical candidate retrieval remains bounded.
