{
  "name": "zenod-monorepo",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w zenod && npm run build -w @zenod/mcp-chassis && npm run build -w @zenod/server && npm run build -w web && npm run build -w site",
    "typecheck": "npm run build -w zenod && npm run build -w @zenod/mcp-chassis && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && node --test scripts/*.test.mjs && npm run schemas:check",
    "schemas:check": "node scripts/build-tool-output-schemas.mjs --check",
    "schemas:build": "node scripts/build-tool-output-schemas.mjs",
    "acceptance:zal17:drive": "node scripts/zal17-drive-journey.mjs",
    "acceptance:zal20:drive": "node scripts/zal20-drive-journey.mjs",
    "test:phylax-image-contract": "bash scripts/zpf2-phylax-image-contract.sh",
    "dev:web": "npm run dev -w web",
    "dev:site": "npm run dev -w site",
    "build:site": "npm run build -w site",
    "dev:server": "npm run dev -w @zenod/server"
  },
  "devDependencies": {
    "@types/node": "^24",
    "esbuild": "^0.28.1",
    "typescript": "~5.9",
    "vitest": "^4.1.10"
  }
}
