{
  "name": "zenod-monorepo",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w zenod && npm run build -w @zenod/server && npm run build -w web",
    "typecheck": "npm run build -w zenod && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:web": "npm run dev -w web",
    "dev:server": "npm run dev -w @zenod/server"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "~5.9",
    "vitest": "^3.2.0"
  }
}
